#python example
name : python

on:
 push:
  branches: labs
   

jobs:
 build:
  runs-on : ubuntu-latest
  
  steps:
  - name : checkout the cide 
    uses : actions/checkout@v3
  - name : install python
    uses:  actions/setup-python@v4  
    with:
      python-version: '3.9'
